/**
 * Promo banner shown on the Home page.
 * Edit this file directly to update the promo message — there's no
 * admin dashboard, so changes here require a save + redeploy.
 */
export const promoConfig = {
  enabled: true,
  heading: "New Drop Every Friday",
  message: "Fresh thrift finds, restocked weekly. Don't sleep on the good pieces.",
  buttonText: "Shop New Arrivals",
  buttonLink: "/new-arrivals",
};